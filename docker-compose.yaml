version: “3.8”

services:
  frontend:   
    stdin_open:  true
    build: 
        context: .
        dockerfile: Dockerfile
    container_name: frontend
    ports:
        -  "3000:3000"
    volumes:
        -  "/app/node_modules"
        -  "./:/app"

